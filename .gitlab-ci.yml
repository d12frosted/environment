image: d12frosted/environment:archlinux

stages:
  - build

variables:
  GIT_CLONE_PATH: $HOME/.config

cache:
  paths:
    - xmonad/.stack-work

.build_template: &build_definition
  stage: build
  before_script:
    - sudo pacman -Syu --noconfirm --needed base-devel git pacman-contrib

build:
  <<: *build_definition
  script:
    - ./eru.sh
