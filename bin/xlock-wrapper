#!/bin/sh

# make sure that US layout is selected
switch_kbd_layout --layout us

# actually lock the screen
# xset s activate

export XSECURELOCK_AUTH_CURSOR_BLINK=0
export XSECURELOCK_PASSWORD_PROMPT=disco
export XSECURELOCK_SHOW_HOSTNAME=0
xsecurelock
