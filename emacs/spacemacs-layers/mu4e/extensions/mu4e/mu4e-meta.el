;; auto-generated
(defconst mu4e-mu-version "0.9.11"
  "Required mu binary version; mu4e's version must agree with this.")

(defconst mu4e-builddir "/private/tmp/mu-HPHXct/mu-0.9.11"
  "Top-level build directory.")

(provide 'mu4e-meta)
