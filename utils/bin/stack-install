#!/usr/bin/env bash

set -e

if [[ "$1" == "" ]]; then
    echo "Argument is not specified"
    exit 1
fi

git_url="$1"

wd="$TMPDIR/$(date "+%Y_%m_%d_%H_%M_%S")"
git clone $git_url $wd
cd "$wd" && {
    stack install
}
